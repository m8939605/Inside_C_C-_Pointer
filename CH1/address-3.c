#include <stdio.h>
int main()
{
    int x = 100, y = 200, z = 300;
    printf("x's address is %p\n", &x);
    printf("y's address is %p\n", &y);
    printf("z's address is %p\n\n", &z);

    getchar();
    return 0;
}
